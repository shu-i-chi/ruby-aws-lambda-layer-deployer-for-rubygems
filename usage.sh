cat <<EOF
Usage:

    $0 [-y | --yes | --skip-prompt]

Options:

    -y, --yes, --skip-prompt
        Automatic yes to prompts;
        assume "yes" as answer to the prompts for confirming the script running settings,
        and run non-interactively.
EOF
